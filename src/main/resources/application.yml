server:
  port: 8081

spring:
  application:
    name: pix project
  data:
    mongodb:
      uri: mongodb://mongodb:27017/transacoes
      auto-index-creation: true
  datasource:
    url: jdbc:mysql://mysql:3306/pix
    username: root
    password: Santosfc@1912
  jpa:
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
          show_sql: true
          format_sql: true
  redis:
    host: redis
    port: 6379
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
  metrics:
    export:
      prometheus:
        enabled: true

management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    prometheus:
      enabled: true
    health:
      enabled: true
      show-details: always
    metrics:
      enabled: true
